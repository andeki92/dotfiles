# Minimal aliases file
# Only includes the most essential aliases

# Navigation
alias ..='cd ..'
alias ...='cd ../..'
alias ll='ls -la'

# Git shortcuts
alias gs='git status'
alias ga='git add'
alias gc='git commit'
alias gcm='git commit -m'
alias gp='git push'

# General
alias c='clear' 
