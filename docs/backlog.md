# Dotfiles Backlog

Cross-cutting dotfiles work not tied to a single module.

## From cbox (2026-04-26 + 2026-04-27)

1. **Notification hook for `Stop` / `Notification` events.** Investigate
   `ntfy.sh` push as a portable replacement for `terminal-notifier`.
2. **Track `~/.claude/settings.json` in dotfiles.** Move file into
   `config/claude/.claude/settings.json` (currently a real file, not a symlink).
3. **Document the Claude Remote Control feature** in the dotfiles README.
